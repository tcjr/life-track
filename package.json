{
  "name": "life-track-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for life-track applications and services",
  "keywords": [],
  "author": "",
  "license": "MIT",
  "scripts": {
    "build": "pnpm --filter=life-track run build",
    "lint": "pnpm --filter '*' run lint",
    "test": "pnpm --filter=life-track run test",
    "start": "pnpm --filter '*' run start",
    "deploy-rules": "firebase deploy --only firestore:rules"
  },
  "pnpm": {
    "overrides": {
      "lightningcss": "1.31.0"
    }
  }
}
